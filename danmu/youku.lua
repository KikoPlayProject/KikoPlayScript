info = {
    ["name"] = "优酷",
    ["id"] = "Kikyou.d.youku",
    ["desc"] = "优酷弹幕脚本",
    ["version"] = "0.2.1",
    ["min_kiko"] = "2.1.0",
    ["label_color"] = "0x2683FF",
    ["icon"] = [[<svg t="1777793046471" class="icon" viewBox="0 0 1024 1024" version="1.1" xmlns="http://www.w3.org/2000/svg" p-id="13998" width="64" height="64"><path d="M740.3 556.4c0.1-0.1 0.1-0.2 0.2-0.3 0.5-0.8 0.9-1.7 1.4-2.5 0.2-0.4 0.4-0.8 0.7-1.2 0.2-0.3 0.3-0.7 0.5-1 5.5-10.8 8.5-22.7 8.6-34.8v-1.2-1.2c-0.2-12.1-3.1-24-8.6-34.8-0.2-0.3-0.3-0.7-0.5-1-0.2-0.4-0.4-0.8-0.7-1.2-0.5-0.8-0.9-1.7-1.4-2.5-0.1-0.1-0.1-0.2-0.2-0.3-1.5-2.5-3.1-4.9-4.9-7.2v-0.1c-2.4-3.2-5.1-6.2-8-9l-0.2-0.2c-1.6-1.5-3.3-3-5-4.4-0.4-0.3-0.7-0.6-1.1-0.9l-3.6-2.7c-0.3-0.2-0.6-0.4-0.8-0.6l-247-165.5c-17.6-11.8-38.8-16.1-59.6-12-20.8 4.1-38.8 16.1-50.6 33.7-24.4 36.4-14.7 85.9 21.7 110.3l148.5 99.6L381.1 615c-36.4 24.4-46.1 73.9-21.7 110.3 11.8 17.6 29.8 29.6 50.6 33.7 20.8 4.1 42-0.2 59.6-12l246.9-165.6c0.3-0.2 0.6-0.4 0.8-0.6l1.5-1c0.7-0.5 1.5-1.1 2.2-1.6 0.4-0.3 0.7-0.6 1.1-0.9 1.8-1.4 3.4-2.9 5-4.4l0.2-0.2c2.9-2.8 5.6-5.8 8-9v-0.1c1.9-2.3 3.5-4.7 5-7.2z m0 0" fill="#FF4A4A" p-id="13999"></path><path d="M975.6 317.5c-25.2-59.6-61.2-113-107.2-158.9-45.9-45.9-99.4-81.9-158.9-107.1-61.7-26.1-127.2-39.3-194.7-39.3-84.7 0-168.4 21.6-242.1 62.4C201.4 114 140.2 171 95.6 239.4c-52.9 81.2-80.9 175.5-80.9 272.8 0 67.5 13.2 133 39.3 194.7 25.2 59.6 61.2 113 107.1 158.9C207 911.7 260.5 947.7 320 973c61.7 26.1 127.2 39.3 194.7 39.3 81.6 0 162.5-20.1 234.2-58.1 69.3-36.8 129.8-90.2 175-154.4 59.5-84.5 90.9-184 90.9-287.6 0.2-67.5-13.1-133-39.2-194.7zM514.8 853.9c-188.4 0-341.7-153.3-341.7-341.7 0-66.5 19.1-130.9 55.2-186.3 63.4-97.3 170.6-155.4 286.5-155.4 188.4 0 341.7 153.3 341.7 341.7 0 70.8-21.5 138.8-62.1 196.4-63.9 91-168.5 145.3-279.6 145.3z m0 0" fill="#38CEFF" p-id="14000"></path><path d="M904.9 689.4c-35.8-25.2-85.2-16.6-110.3 19.2-25.2 35.8-16.6 85.2 19.2 110.3 13.9 9.8 29.8 14.4 45.5 14.4 24.9 0 49.4-11.7 64.8-33.6 25.1-35.7 16.5-85.1-19.2-110.3zM118.7 349c36.6 23.9 85.7 13.5 109.6-23.1 23.9-36.6 13.5-85.7-23.1-109.6-36.6-23.9-85.7-13.5-109.6 23.1C71.7 276 82.1 325.1 118.7 349z m0 0" fill="#0B99FF" p-id="14001"></path><path d="M751.7 514.2c-0.2-12.1-3.2-24-8.6-34.8-0.2-0.3-0.3-0.7-0.5-1-0.2-0.4-0.4-0.8-0.7-1.2-0.5-0.8-0.9-1.7-1.4-2.5-0.1-0.1-0.1-0.2-0.2-0.3-0.7-1.1-1.3-2.2-2.1-3.2-0.9-1.4-1.9-2.7-2.9-4v-0.1c-2.5-3.2-5.1-6.2-8-9l-0.2-0.2c-1.6-1.5-3.3-3-5-4.4-0.4-0.3-0.7-0.6-1.1-0.9-0.7-0.6-1.4-1.1-2.2-1.6-0.5-0.3-1-0.7-1.5-1-13.3-9.2-29-14.1-45.2-14-15.8 0-31.1 4.7-44.2 13.5l-98.4 66 98.4 66c13.1 8.8 28.4 13.5 44.2 13.5 16.4 0 32.1-5 45.2-14l1.5-1c0.7-0.5 1.5-1.1 2.2-1.6 0.4-0.3 0.7-0.6 1.1-0.9 1.7-1.4 3.4-2.9 5-4.4l0.2-0.2c2.9-2.8 5.5-5.7 8-9v-0.1c1-1.3 1.9-2.6 2.9-4 0.7-1.1 1.4-2.2 2.1-3.2 0.1-0.1 0.1-0.2 0.2-0.3 0.5-0.8 1-1.7 1.4-2.5 0.2-0.4 0.4-0.8 0.7-1.2 0.2-0.3 0.3-0.7 0.5-1 5.5-10.8 8.4-22.7 8.6-34.8v-1.2-1.4z m0 0" fill="#F5001A" p-id="14002"></path></svg>]],
}

supportedURLsRe = {
    "(https?://)?v.youku.com/v_show/id_(.+)\\.html\\??(.*)?"
}

sampleSupporedURLs = {
    "https://v.youku.com/v_show/id_XNTkxMjQxNzM4MA==.html"
}

settings = {
    ["skip_ad_time"]={
        ["title"]="跳过广告时长(s)",
        ["desc"]="跳过广告时长(s)，弹幕播放时间会减去这个时长",
        ["default"]="0"
    },
    ["user_agent"]={
        ["title"]="UA",
        ["desc"]="User Agent",
        ["default"]="Mozilla/5.0 (Windows NT 10.0; Win64; x64) Gecko/20100101 Firefox/108.0"
    }
}

function get_ts(date_str)
    --date_str = "2022-10-17 18:23:26"
    local year, month, day, hour, min, sec = date_str:match("(%d+)-(%d+)-(%d+) (%d+):(%d+):(%d+)")
    local timestamp = os.time({
        year = tonumber(year),
        month = tonumber(month),
        day = tonumber(day),
        hour = tonumber(hour),
        min = tonumber(min),
        sec = tonumber(sec),
    })
    return timestamp
end

function escape(str)
   str = string.gsub (str, "([^0-9a-zA-Z !'()*._~-])", -- locale independent
      function (c) return string.format ("%%%02X", string.byte(c)) end)
   str = string.gsub (str, " ", "+")
   return str
end

function get_cookies(cookie_str)
    local cookies = {}
    if cookie_str == nil then return cookies end
    local cookie_rows = string.split(cookie_str, '\n')
    for _, cookie_row in ipairs(cookie_rows) do
        local cookie_items = string.split(cookie_row, ';')
        if #cookie_items > 0 then
            local kvs = string.split(cookie_items[1], '=')
            if #kvs > 1 then
                cookies[kvs[1]] = kvs[2]
            end
        end
    end
    return cookies
end

function get_video_info(vid)
    local info_url = string.format("https://openapi.youku.com/v2/videos/show.json?client_id=53e6cc67237fc59a&package=com.huawei.hwvplayer.youku&ext=show&video_id=%s", vid)
    local err, reply = kiko.httpget(info_url, {}, {["User-Agent"] = settings["user_agent"]})
    if err ~= nil then error(err) end
    local err, obj = kiko.json2table(reply["content"])
    if err ~= nil then error(err) end
    if obj["duration"] == nil then error("未找到视频时长") end
    local duration = tonumber(obj["duration"])
    local title = obj["title"]
    if title == nil then title = "unknown" end
    return title, duration
end

function download_seg(cna, vid, seg, danmuList)
    local msg = {
        ["ctime"] = tostring(os.time() * 1000),
        ["ctype"] = 10004,
        ["cver"] = "v1.0",
        ["guid"] = cna,
        ["mat"] = seg,
        ["mcount"] = 1,
        ["pid"] = 0,
        ["sver"] = "3.1.0",
        ["type"] = 1,
        ["vid"] = vid
    }
    local err, msg_json = kiko.table2json(msg, "compact")
    local err, msg_b64 = kiko.base64(msg_json, "to")
    msg["msg"] = msg_b64
    local err, sign = kiko.hashdata(msg_b64 .. "MkmC9SoIw6xCkSKHhJ7b5D2r51kBiREr", false)
    msg["sign"] = sign
    local err, reply = kiko.httpget("https://acs.youku.com/h5/mtop.com.youku.aplatform.weakget/1.0/?jsv=2.5.1&appKey=24679788", {}, {["User-Agent"] = settings["user_agent"]})
    if err ~= nil then
        kiko.log(string.format("vid: %s, seg: %d, http error: %s", vid, seg, err))
        return danmuList
    end
    local cookies = get_cookies(reply["headers"]["Set-Cookie"])
    if cookies["_m_h5_tk_enc"] == nil or cookies["_m_h5_tk"] == nil then 
        kiko.log(string.format("vid: %s, seg: %d, error: Cookie not found, %s", vid, seg, err))
        return danmuList
    end
    local cookie_header = ""
    for k, v in pairs(cookies) do
        local c = string.format("%s=%s", k, v)
        if #cookie_header == 0 then
            cookie_header = c
        else
            cookie_header = cookie_header .. ";" .. c
        end
    end
    local headers = {
        ["Content-Type"] = "application/x-www-form-urlencoded",
        ["Cookie"] = cookie_header,
        ["Referer"] = "https://v.youku.com",
        ["User-Agent"] = settings["user_agent"]
    }
    local err, data = kiko.table2json(msg, "compact")
    local t = tostring(os.time() * 1000) 
    local t_sing_hash_data = string.sub(cookies["_m_h5_tk"], 1, 32) .. "&" .. t .. "&" .. "24679788" .. "&" .. data
    local err, t_sign = kiko.hashdata(t_sing_hash_data, false)
    local params = {
        ["jsv"] = "2.5.6",
        ["appKey"] = "24679788",
        ["t"] = t,
        ["sign"] = t_sign,
        ["api"] = "mopen.youku.danmu.list",
        ["v"] = "1.0",
        ["type"] = "originaljson",
        ["dataType"] = "jsonp",
        ["timeout"] = "20000",
        ["jsonpIncPrefix"] = "utility"
    }
    local err, reply = kiko.httppost("https://acs.youku.com/h5/mopen.youku.danmu.list/1.0/", "data=" .. escape(data), headers, params)
    if err ~= nil then
        kiko.log(string.format("vid: %s, seg: %d, error: %s", vid, seg, err))
        return danmuList
    end
    local err, obj = kiko.json2table(reply["content"])
    if err ~= nil then
        kiko.log(string.format("dm content error, vid: %s, seg: %d, error: %s", vid, seg, err))
        return danmuList
    end
    local err, dobj = kiko.json2table(obj["data"]["result"])
    if err ~= nil then
        kiko.log(string.format("dmobj error, vid: %s, seg: %d, error: %s", vid, seg, err))
        return danmuList
    end
    local skip_time = tonumber(settings["skip_ad_time"]) * 1000
    for _, dm in ipairs(dobj["data"]["result"]) do
        local color = 0xffffff
        local size = 0
        local err, pobj = kiko.json2table(dm["propertis"])
        if err==nil then
            if pobj["color"] ~= nil then
                color = tonumber(pobj["color"])
            end
            if pobj["size"] ~= nil and tonumber(pobj["size"]) > 2 then
                size = 2
            end
        end    
        table.insert(danmuList, {
            ["text"]=dm["content"],
            ["time"]=dm["playat"]-skip_time,
            ["date"]=tostring(get_ts(dm["createtime"])),
            ["color"]=color,
            ["fontsize"]=size,
            ["sender"]="[Youku]" .. dm["uid2"]
        })
    end
    return danmuList
end

function urlinfo(url)
    local reg = kiko.regex("[\\s\\S]+?youku.com/v_show/id_(.+?)\\.html")
    local _, _, vid = reg:find(url)
    
    if vid == nil then error("不支持的URL") end
    local title, duration = get_video_info(vid)
    if title == nil then title = "unknown" end
    
    local data = { ["vid"] = vid, ["duration"] = duration }
    local _, data_str = kiko.table2json(data, "compact")

    local results = {}
    table.insert(results, {
        ["title"] = title,
        ["duration"] = duration,
        ["data"] = data_str
    })
    return results
end

function danmu(source)
    local err, source_obj = kiko.json2table(source["data"])
    if err ~= nil then error(err) end

    local err, reply = kiko.httpget("https://log.mmstat.com/eg.js", {}, {["User-Agent"] = settings["user_agent"], ["Accept-Encoding"] = "gzip, deflate", ["Accept"] = "*/*", ["Connection"] = "keep-alive"})
    local cookie_header = reply["headers"]["Set-Cookie"] or reply["headers"]["set-cookie"]
    local cookies = get_cookies(cookie_header)
    if cookies["cna"] == nil then error("get cna cookie failed") end

    local segs = math.floor(source_obj["duration"] / 60) + 1
    local danmuList = {}
    for i = 1,segs do 
        download_seg(cookies["cna"], source_obj["vid"], i, danmuList)
    end
    return nil, danmuList
end
